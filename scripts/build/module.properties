#admin or site
build.modules = modules

#Module folder
build.module = ${build.modules}/${phing.project.name}

#Main xml manifest filename
build.manifest = ${phing.project.name}.xml

#Main xml manifest
build.manifestdir = ${build.code}/${build.module}/${build.manifest}

#Tmp xml manifest
build.tmpmanifestdir = ${build.tmpdir}/${build.module}/${build.manifest}